# Dotfiles

## Installing

  - Backup all your dotfiles
  - Create your gitconfig.local
    ```
    touch ~/.gitconfig.local
    ```

    Then add your informations

    ```
    [user]
      name = "Your Name"
      email = "your@email"
    ```
  - Run `./install`
